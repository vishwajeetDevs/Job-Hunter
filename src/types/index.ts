export type NavItem = {
  label: string;
  href: string;
};

export type Feature = {
  title: string;
  description: string;
  icon: string;
};

export type Step = {
  step: number;
  title: string;
  description: string;
};
